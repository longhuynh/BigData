import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.mapreduce.Partitioner;

public class HybridPartitioner extends Partitioner<Pair, IntWritable> {

	@Override
	public int getPartition(Pair p, IntWritable p1, int numReduceTasks) {
		/*String chrs="abcdefghij";
		boolean vlid = false;
		for(int i =0 ; i < chrs.length(); i++){
			if(p!= null && p.toString().toLowerCase().charAt(0) == chrs.charAt(i)){
				vlid = true;
				break;
			}
		}
		return vlid == true ? 0 : 1;*/
		return 0;
	}

}
